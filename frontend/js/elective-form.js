// Your existing elective-form.js content, but update API calls to use the new structure:
// Change: fetch(`${API_URL}/elective-courses`) 
// To: SmartScheduleUtils.apiCall('/elective/courses')

// Also update all other API endpoints to match the new route structure